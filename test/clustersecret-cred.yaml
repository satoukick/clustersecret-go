apiVersion: clustersecret.io/v1
kind: ClusterSecret
metadata:
  name: shared-creds
spec:
  matchNamespace:
    - "^team-.*"
  data:
    api-key: "supersecret"
    db-password: "p@ssw0rd"
  type: Opaque
